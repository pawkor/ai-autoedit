user root;
worker_processes 4;
events { worker_connections 1024; }

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    sendfile        on;
    tcp_nopush      on;
    keepalive_timeout 65;
    gzip on;
    gzip_types text/plain text/css application/json application/javascript;

    # WebSocket upgrade map
    map $http_upgrade $connection_upgrade {
        default upgrade;
        ''      '';
    }

    # Upstream uvicorn
    upstream app {
        server autoframe:8000;
        keepalive 32;
    }

    server {
        listen 443 ssl;
        http2 on;

        ssl_certificate     /etc/nginx/certs/cert.pem;
        ssl_certificate_key /etc/nginx/certs/key.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         HIGH:!aNULL:!MD5;

        location ^~ /js/ {
            root /app/webapp/static;
            include /etc/nginx/mime.types;
            add_header Cache-Control "no-cache, must-revalidate";
        }

        location ^~ /css/ {
            root /app/webapp/static;
            include /etc/nginx/mime.types;
            add_header Cache-Control "no-cache, must-revalidate";
        }

        # Fonts: long cache (content never changes)
        location ~* \.(woff2?)$ {
            proxy_pass http://app;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header Connection "";
            add_header Cache-Control "public, max-age=604800";
        }

        # Serve extracted frames directly from disk (no Python involved)
        location ~* /_autoframe/frames/[^/]+\.jpg$ {
            root /;
            add_header Cache-Control "public, max-age=604800";
        }

        # Serve result/source video files directly from /data (sendfile, native range requests)
        location ~* ^/data/.*\.(mp4|mov|webm|m4v|mkv)$ {
            root /;
            add_header Cache-Control "public, max-age=86400";
            add_header Accept-Ranges bytes;
        }

        location / {
            proxy_pass http://app;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;
            proxy_read_timeout 3600s;
            proxy_send_timeout 3600s;
            add_header Cache-Control "no-cache, must-revalidate";
        }
    }

    # Redirect HTTP → HTTPS
    server {
        listen 80;
        return 301 https://$host$request_uri;
    }
}
