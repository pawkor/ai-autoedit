# Web app
fastapi==0.135.3
uvicorn[standard]==0.34.0
aiofiles==25.1.0
python-multipart==0.0.31
websockets==14.2

# AI pipeline
# torch+cu128: installed via --extra-index-url in Dockerfile
torch==2.11.0+cu128
torchvision==0.26.0+cu128
open_clip_torch==3.3.0
transformers>=4.40.0
opencv-python-headless==4.13.0.92
scenedetect==0.6.7.1
pandas==3.0.1
librosa==0.11.0
soundfile==0.13.1
numpy==2.4.4
tqdm==4.67.3
Pillow==12.2.0
ffmpeg-python==0.2.0
psutil==7.2.2
anthropic==0.89.0
google-api-python-client==2.131.0
google-auth-oauthlib==1.2.0
google-auth-httplib2==0.2.0
boto3==1.35.99
yt-dlp==2026.3.17
prometheus_client==0.21.1

# Privacy & OCR
ultralytics>=8.3.0
easyocr>=1.7.1

# Beat detection (optional — better tempo tracking for music-driven; active after container rebuild)
BeatNet>=1.1.1

# Vocal separation (optional — replaces HPSS harmonic for harm_energy onset accuracy)
demucs>=4.0.1

# whisperx (chorus detection) excluded — incompatible with torchaudio 2.11+cu128
# (AttributeError: list_audio_backends / AudioMetaData removed in torchaudio 2.x)
# Code in music_driven.py handles ImportError gracefully (returns [] silently)
